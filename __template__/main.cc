#include <bits/stdc++.h>
using namespace std;
#define D(x) cerr << #x << " = " << (x) << ", "

int main()
{
    int tests;
    cin >> tests;
    for (int test = 0; test < tests; ++test) {
        
    }
}
