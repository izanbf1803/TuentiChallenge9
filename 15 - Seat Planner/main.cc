#include <bits/stdc++.h>
using namespace std;
#define D(x) cout << #x << " = " << x << ", "

int main(int argc, char** argv)
{
    istringstream in(argv[1]);
    int S, K;
    in >> S >> K;
    for (int i = 0; i < K; ++i) {
        int a, b;
        in >> a >> b;
    }
    cout << endl;
}